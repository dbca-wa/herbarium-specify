apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: specify-storage
spec:
  storageClassName: azurefile-csi  # Azure Files, not Blob
  accessModes:
  - ReadWriteMany  # Multiple pods/nodes can mount
  resources:
    requests:
      storage: 2Gi